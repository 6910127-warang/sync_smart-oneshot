# SmartSync

**ไทย:** ระบบสนับสนุนการเบิกยา SmartSync เครือข่าย รพ.สต. อำเภอเมือง จังหวัดเชียงราย
**English:** SmartSync — A Requisition Support System for Inventory Balance in HPH Networks, Mueang Chiang Rai

## บริบทโครงการ (Project Context)

- **หน่วยงานเจ้าของระบบ:** กลุ่มงานเภสัชกรรม โรงพยาบาลเชียงรายประชานุเคราะห์ (รพ.แม่ข่าย)
- **หน่วยงานที่ใช้งาน (Network):** โรงพยาบาลส่งเสริมสุขภาพตำบล (รพ.สต.) 29 แห่งในอำเภอเมือง จังหวัดเชียงราย
- **เจ้าของข้อกำหนด (Requirement Owner):** เภสัชกร รพ.เชียงรายประชานุเคราะห์ (ผู้ใช้งาน Claude Code ในโปรเจกต์นี้)
- **Repository:** [github.com/6910127-warang/sync_smart-oneshot](https://github.com/6910127-warang/sync_smart-oneshot.git) (branch: `main`) — commit/push เฉพาะเมื่อผู้ใช้ขอเท่านั้น (แก้ไข 20260920: ของเดิมชี้ไป `sync_smart` ซึ่งเป็นคนละ repo กับที่ local ใช้งานจริงมาตลอด — ดูหัวข้อ "เฟสปัจจุบัน" อัปเดต 20260920)

**ปัญหาที่ต้องการแก้ไข และ KPI ของโครงการ** อยู่ใน spec หลักที่ [01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md) — สรุปสั้นๆ: ผู้เบิก (เจ้าหน้าที่ รพ.สต.) และผู้คัดกรอง (เภสัชกร) ใช้เกณฑ์คำนวณยอดเบิกคนละมาตรฐานกัน ทำให้อัตราหมุนเวียนคงคลังสูงถึง 3 เดือน (เป้าหมาย ≤ 1.5 เดือน) **อย่าสัมภาษณ์ผู้ใช้ซ้ำเรื่องนี้** — อ่านไฟล์นั้นก่อนถ้าต้องการบริบทปัญหา

## เฟสปัจจุบัน (Current Phase)

**โฟกัสหลักตอนนี้คือการทำเอกสาร ไม่ใช่การเขียนโค้ด** — อย่าเริ่มออกแบบ schema ฐานข้อมูล เขียนโค้ด หรือเลือก framework จริงจัง จนกว่าจะมีคำสั่งชัดเจนว่าจะเข้าสู่เฟสพัฒนา **ข้อยกเว้นเดียว:** Prototype แบบ static HTML/CSS ภายใต้ `prototype/` (ดูด้านล่าง) — เป็น mockup คลิกดูได้เพื่อตรวจสอบความเข้าใจร่วมกับผู้ใช้จริงก่อนเข้าเฟสพัฒนา ไม่ใช่แอปที่ใช้งานจริง (ไม่มี framework/backend/state จริง) จึงไม่ถือว่าขัดกับกฎ "ไม่เขียนโค้ด" ข้อนี้

**อัปเดต 20260901:** ผู้ใช้ยืนยันให้เข้าสู่เฟสพัฒนาจริงแบบเจาะจงฟีเจอร์แรก — หน้า "รายการคำขอเบิกของหน่วยฉัน" (BL-002/BL-004/BL-005, FT-002) เชื่อมกับฐานข้อมูลจริง **Firebase (Cloud Firestore)** โค้ด/schema จริงอยู่ที่โฟลเดอร์ `app/` (แยกจาก `prototype/` ซึ่งยังคงเป็น static mockup ที่ดูแลโดย `/build-prototype` ตามเดิม ห้ามนำโค้ดจริงไปแก้ไฟล์ในนั้น) — กฎ "เอกสารก่อนโค้ด" ด้านบนยังคงบังคับใช้กับฟีเจอร์/หน้าจออื่นที่ยังไม่ได้รับคำสั่งเข้าสู่เฟสพัฒนาเช่นเดียวกัน ไม่ถือว่าเปิดเฟสพัฒนาทั้งโปรเจกต์โดยอัตโนมัติ — ต้องขอคำสั่งเจาะจงทีละฟีเจอร์ต่อไป

**อัปเดต 20260907:** ผู้ใช้ขอฟีเจอร์ที่สองแบบเจาะจง — หน้าจอ role เภสัชกร **เฉพาะขอบเขตอนุมัติระดับ 1** (คิว + พิจารณา/อนุมัติ/ปฏิเสธ, BL-004) โค้ดจริงอยู่ที่ `app/pharmacist/approval-queue-level1.html` และ `approval-review-level1.html` — ระดับ 2, ส่งออก Excel (BL-020), audit-trail **ยังไม่ได้รับคำสั่งเข้าสู่เฟสพัฒนา** เช่นเดียวกับ BL-032/BL-015 เต็มรูปแบบที่ต้องรอ Epic 2 (ดูรายละเอียดใน `app/README.md` หัวข้อ "ขั้นต่อไป") — เช่นเดิม ไม่ถือว่าเปิดเฟสพัฒนาทั้ง role หรือทั้งโปรเจกต์โดยอัตโนมัติ

**อัปเดต 20260907 (บ่าย):** ผู้ใช้ขอต่อ **Firebase Authentication จริง (BL-024, email/password)** ให้ทั้งสอง role พร้อมกัน แทนกลไกล็อก session ชั่วคราวผ่าน `localStorage` เดิม (`lib/session.js`, `lib/pharmacist-session.js`, `lib/pharmacists.js` — ลบทั้งสามไฟล์แล้ว) โค้ดจริงอยู่ที่ `app/lib/auth.js` (ใช้ร่วมกันทั้งสอง role) รายละเอียด/ขั้นตอนที่เหลือ (ต้องเปิด provider Email/Password ใน Firebase Console เอง, ยังไม่บังคับใช้ `mustChangePassword`/`twoFactorEnabled`, ยังไม่มีหน้า admin สร้างบัญชี — ใช้ `seed.html` แทนไปก่อน) ดูใน `app/README.md` หัวข้อ "Firebase Authentication" และ "ขั้นต่อไป" — Firestore Security Rules ที่กรองตาม `role`/`unitId` จริง (ต่างจากกฎเดียว "ต้อง login" ที่ร่างไว้ใน `firestore.rules` แล้ว) **ยังไม่ได้รับคำสั่งเข้าสู่เฟสพัฒนา** เช่นเดิม

**อัปเดต 20260908:** ระหว่างทดสอบ BL-024 พบบั๊ก cross-tab (login หน้าเภสัชกรถูกต้องแล้ว แต่เปิดหน้า staff-hph ค้างไว้อีกแท็บทำให้หลุด login วนไม่จบ เพราะ `watchAuth` เดิม sign out อัตโนมัติเมื่อ role ไม่ตรง ซึ่งไปกระทบทุกแท็บเพราะ Firebase Auth ใช้ persistence เดียวกันข้าม origin) แก้แล้วโดยไม่ sign out อัตโนมัติอีกต่อไป — ดูรายละเอียดใน `app/README.md` หัวข้อ "Firebase Authentication" ในโอกาสเดียวกันผู้ใช้ขอรวมฟอร์ม login ที่เคยฝังซ้ำอยู่ใน 4 หน้าจอให้เหลือจุดเดียว — เพิ่ม `app/login.html` เป็นจุดเข้า login กลาง login แล้ว redirect ไปหน้าแรกของ role นั้นอัตโนมัติ ทั้ง 4 หน้าจอเดิมเหลือแค่ guard ผ่าน `watchAuth` (ไม่มีฟอร์ม login ในตัวเองแล้ว)

**อัปเดต 20260908 (บ่าย):** ผู้ใช้ขอต่อ **Firestore Security Rules ที่กรองตาม `role`/`unitId` จริง** (แทนกฎเดิม "แค่ต้อง login") — เขียนแล้วที่ [`firestore.rules`](firestore.rules) ครอบคลุมเฉพาะ collection/การกระทำที่มีหน้าจอจริงใช้งานอยู่ตอนนี้ (BL-001/002/004/005/024, อนุมัติระดับ 1 เท่านั้น) การเพิ่มกฎนี้ทำให้ต้องแก้โค้ดจริงตามไปด้วย 2 จุด: `requisition-new.html` (แยกการเขียน `requisitions` doc ออกจาก batch ของ `lineItems` เพราะ security rule ต้อง `get()` อ่านคำขอแม่กลับมาเช็คแต่ Firestore ไม่การันตีเห็นงานเขียนอื่นในคำขอ/batch เดียวกัน) และ `requisition-list.html` (เพิ่ม filter `receivingUnitId` ให้ query ของ `goodsReceiptRecords` ตรงกับเงื่อนไขใน rule) — รายละเอียดกฎ/ผลกระทบต่อ `seed.html`/ขั้นตอน publish จริงดูใน `app/README.md` หัวข้อ "ความปลอดภัย" **สำคัญ: ยังไม่ได้ publish กฎนี้ผ่าน Firebase Console จริง** (โปรเจกต์ไม่มี Firebase CLI ให้ deploy อัตโนมัติ) — ยังไม่ถือว่าปลดล็อกให้ deploy ให้หน่วยงานจริงใช้งานได้จนกว่าจะ publish + ทดสอบ RBAC จริงตาม checklist ใน README

**อัปเดต 20260914:** ผู้ใช้ตัดสินใจย้าย static hosting ของ `app/` จาก Firebase Hosting ไป **Cloudflare** (ทดลองตั้งเป็น Hostinger ช่วงเช้าก่อน แล้วเปลี่ยนมาเป็น Cloudflare ช่วงบ่ายวันเดียวกัน) โดยเชื่อม GitHub repo โดยตรงเพื่อให้ deploy อัตโนมัติทุกครั้งที่ push ขึ้น `main` — Live URL ปัจจุบัน (ยืนยันใช้งานได้จริงครบทุกจุดรวม login ด้วยบัญชีทดสอบ): `https://sync-smart.thiphbuymepharmacy.workers.dev` — ของเดิม `https://syncsmart-98d1e.web.app` (Firebase Hosting) **ยังปล่อยขนานกันไว้ ใช้งานได้ทั้งคู่พร้อมกัน** (ทั้งสองชี้ไป Firestore/Auth โปรเจกต์ `syncsmart-98d1e` เดียวกัน ไม่กระทบ backend) ไม่ได้ผูก custom domain เอง (ใช้ URL `*.workers.dev` ที่ Cloudflare สร้างให้ตรงๆ) รายละเอียดขั้นตอน deploy/build settings เต็มดูใน `app/README.md` หัวข้อ "Hosting — ย้ายไป Cloudflare" — เหมือนเดิม ไม่ถือว่าเปิดเฟสพัฒนาส่วนอื่นเพิ่มเติม เป็นแค่การย้าย hosting ของโค้ดที่มีอยู่แล้ว

**อัปเดต 20260915:** ผู้ใช้ขอฟีเจอร์ที่สามแบบเจาะจง — **ผู้ช่วย AI สำหรับเภสัชกรผู้อนุมัติระดับ 1** (FT-036/BL-047, spec เต็มที่ [20260914-008-pharmacist-approval-ai-assistant.md](01-requirements/01-spec/20260914-008-pharmacist-approval-ai-assistant.md)) — ความสามารถ (ก) สรุปเหตุผลความเบี่ยงเบนยอดขอเบิก vs ยอดแนะนำ และ (ข) ช่วยร่างข้อความเหตุผลตอนปฏิเสธ โค้ดจริงอยู่ที่ `app/pharmacist/approval-review-level1.html`/`approval-queue-level1.html`, `app/lib/ai-assistant.js`, และ **`app/_worker.js`/`app/wrangler.toml` (Cloudflare Worker proxy — backend component แรกของโปรเจกต์)** เพื่อไม่ให้คีย์ OpenRouter ฝังอยู่ในเว็บสาธารณะ — รายละเอียด/ขั้นตอน manual ที่เหลือ (ตั้ง secret, ตรวจชื่อโปรเจกต์ Cloudflare) และขอบเขตที่ตัดออกในรอบนี้ (ไม่มีประวัติ 3 ปี, เกณฑ์ trigger % hardcode 25%, ความสามารถ (ข) จำกัดเฉพาะฟอร์มปฏิเสธ) ดูใน `app/README.md` หัวข้อ "ผู้ช่วย AI สำหรับเภสัชกรผู้อนุมัติระดับ 1" — เหมือนเดิม ไม่ถือว่าเปิดเฟสพัฒนาทั้ง role หรือทั้งโปรเจกต์โดยอัตโนมัติ (ระดับ 2, ส่งออก Excel, audit-trail, หน้า Admin ตั้งค่าระบบ ฯลฯ ยังไม่ได้รับคำสั่ง)

**อัปเดต 20260915 (ค่ำ):** ผู้ใช้ถามว่ามีบันทึกการทำงานของผู้ช่วย AI ใน Firestore ให้เปิดดูใน Console หรือยัง (ตอนนั้นยังไม่มี) แล้วขอ requirement ใหม่ + สั่งเข้าสู่เฟสพัฒนาทันที — **บันทึกการใช้งานผู้ช่วย AI** (BL-048/FR-8.7-FR-8.11, สัมภาษณ์ผ่าน `AskUserQuestion` ครบ 6 ข้อ ผลสรุปอยู่ใน spec 008 หัวข้อ "เพิ่มเติม 20260915 (รอบ 5)") โค้ดจริงอยู่ที่ `app/lib/ai-assistant.js` (`logAiAssistantUsage`, เขียนจากฝั่ง client หลังเรียกผู้ช่วย AI สำเร็จ) เขียนลง collection ใหม่ `aiAssistantUsageLogs` (อ่านได้เฉพาะ admin, เขียนได้เฉพาะเจ้าของบันทึกเอง, immutable, เก็บถาวรไม่มีกำหนดลบ) พร้อมกฎเพิ่มใน `firestore.rules` (publish แล้ว 20260915 ผ่าน `firebase.cmd deploy --only firestore:rules`) — รายละเอียด schema ดูใน `app/README.md` หัวข้อ "บันทึกการใช้งานผู้ช่วย AI" — เหมือนเดิม ไม่ถือว่าเปิดเฟสพัฒนาส่วนอื่นของฟีเจอร์ผู้ช่วย AI หรือทั้งโปรเจกต์โดยอัตโนมัติ

**อัปเดต 20260920:** ผู้ใช้ขอให้ตรวจสอบว่า Cloudflare deploy ล่าสุด (หลัง commit `6c079eb`) ขึ้นจริงหรือยัง — พบว่า **auto-deploy ผ่าน GitHub ไม่ทำงานมาตั้งแต่หลัง commit `9b9d979` (ประมาณ 4 วันก่อนหน้า)** สาเหตุคือตอนตั้งค่า Cloudflare Builds ช่วง 20260914 เชื่อมผิด repo — เชื่อมไปที่ `6910127-warang/sync_smart` (repo พี่น้องที่ไม่ได้ใช้งานจริง) แทนที่จะเป็น `6910127-warang/sync_smart-oneshot` (repo ที่ local ทุก session push จริงมาตลอด และผู้ใช้ยืนยันแล้วว่าเป็น repo หลักตัวจริงตามตั้งใจ ไม่ใช่ของค้าง) ทำให้ live site ตกค้างไป 6 commit (`a81d56c`…`6c079eb`) โดยไม่มีอาการฟ้อง error ใดๆ ใน Cloudflare dashboard เลย (build ล่าสุดที่ auto-trigger จริงคือของ `9b9d979`) — แก้ปัญหาเฉพาะหน้าแล้วโดย deploy ตรงด้วย `npx wrangler deploy` จาก working copy ของ `app/` (ตัด `seed.html`/`ai-test.html` ออกก่อนตามขั้นตอน CI ปกติ ไม่กระทบไฟล์ dev จริงในเครื่อง) — Live URL ปัจจุบันอัปเดตครบแล้ว (Version ID `90cc4381-a6a3-467d-8b42-640f678deb8d`) **แต่ยังไม่ได้แก้ที่ต้นเหตุ** — ต้องเข้าไปที่ Cloudflare dashboard → Worker `sync-smart` → Settings → Builds → Manage แล้วเปลี่ยน Git repository ที่เชื่อมจาก `sync_smart` เป็น `sync_smart-oneshot` เองเท่านั้น (ไม่มี API scope ให้ Claude Code แก้ผ่าน wrangler token ที่มีอยู่ในเครื่อง — ต้องทำผ่าน UI ที่ login แล้วเท่านั้น) จนกว่าจะแก้จุดนี้ `git push` ครั้งต่อไปจะยังไม่ auto-deploy เหมือนเดิม

### สถาปัตยกรรมและคำสั่งที่ใช้ใน `app/` (โค้ดจริง)

ไม่มี build tool/package manager/framework ใดๆ ในโปรเจกต์นี้ทั้งใน `app/` และ `prototype/` — เป็น static HTML/CSS + JS แบบ ES module ล้วนๆ, เชื่อม Firestore ตรงจาก browser ผ่าน Firebase JS SDK ที่ import จาก CDN (`https://www.gstatic.com/firebasejs/...`) ไม่มี `package.json`, ไม่มีขั้นตอน compile/bundle/lint/test แยกต่างหาก — แก้ไฟล์แล้ว refresh browser ดูผลได้ทันที **ข้อยกเว้นเดียว (เพิ่ม 20260915, FT-036/BL-047):** `app/_worker.js`/`app/wrangler.toml` เป็น Cloudflare Worker — backend component แรกของโปรเจกต์ ทำหน้าที่ proxy คำขอไปยัง OpenRouter (ผู้ช่วย AI) เท่านั้น ไม่กระทบสถาปัตยกรรม static ของหน้าจออื่นๆ เลย — ดูรายละเอียดใน `app/README.md` หัวข้อ "ผู้ช่วย AI สำหรับเภสัชกรผู้อนุมัติระดับ 1"

- **โครงสร้าง:** `app/{role-slug}/{screen-slug}.html` หนึ่งหน้าจอต่อไฟล์ (เช่น `app/staff-hph/requisition-list.html`), `app/assets/` เก็บ `tokens.css`/`components.css`/`nav.js` ที่ใช้ร่วมกันทุกหน้า (ต้องสอดคล้องกับ design token เดียวกับ `DESIGN.md`/`prototype/`), `app/lib/` เก็บ JS logic ที่ใช้ร่วมกันข้ามหน้าจอ (เช่น `requisition-code.js` สร้างรหัสคำขอแบบอ่านง่าย), `app/firebase-config.js` เก็บ Firebase project config (เป็น client-side identifier ไม่ใช่ secret แต่ยังต้องพึ่ง Firestore Security Rules เพื่อความปลอดภัย — ดูด้านล่าง), `app/seed.html` เป็นเครื่องมือ dev ใส่ข้อมูลตัวอย่างเพื่อทดสอบเท่านั้น ไม่ใช่ส่วนหนึ่งของแอปจริง
- **รันดูในเครื่อง:** ใช้ config ที่มีอยู่แล้วใน `.claude/launch.json` — `prototype` serve โฟลเดอร์ `prototype/` ที่ port 4173, `app` serve โฟลเดอร์ `app/` ที่ port 4174 เปิดผ่าน local server เสมอ อย่าเปิดไฟล์ตรงๆ ด้วย `file://` เพราะ relative path บางส่วน resolve ไม่ได้ — ทั้งคู่รันผ่าน [`.claude/no-cache-server.py`](.claude/no-cache-server.py) (เพิ่ม 20260908 แทน `py -m http.server` เดิม) ซึ่งเป็น wrapper บาง ๆ ของ `http.server.SimpleHTTPRequestHandler` มาตรฐาน เพิ่มแค่ header `Cache-Control: no-store` — เพราะ `py -m http.server` เดิมไม่ส่ง cache header ใดๆ ทำให้ browser แคช `.js`/`.html` เก่าไว้เงียบๆ ข้ามวันได้ (เจอปัญหาจริงตอนทดสอบ BL-024 20260907-08 ที่ hard refresh บางครั้งก็ยังไม่พอ) ไม่นับเป็น build tool/framework เพิ่ม เป็นแค่ stdlib เพิ่ม header เดียว สถาปัตยกรรม static ล้วนๆ ยังเหมือนเดิมทุกอย่าง
- **Firestore schema/collection ทั้งหมด (field-level) และรายการ "ขั้นต่อไปที่ยังไม่ทำ" อยู่ใน [`app/README.md`](app/README.md) เท่านั้น — ต้องอ่านไฟล์นั้นก่อนแก้โค้ดใน `app/` เสมอ** เพราะจะหลุด sync กันได้ง่ายเหมือนที่เคยเกิดกับ open items ด้านล่าง (สรุปสั้นที่สุด: field ฝั่ง Firestore ใช้ `camelCase` แปลตรงจาก field เชิงแนวคิดใน `01-requirements/06-data-model.md`, ทุกฟิลด์ implementation-only ที่ไม่มีในเอกสารเชิงแนวคิดต้องมีหมายเหตุกำกับไว้ในไฟล์นั้นว่าทำไมถึงเพิ่ม) รายการ collection/สถานะด้านล่างเป็น quick reference เท่านั้น — ถ้าไม่ตรงกับ `app/README.md` ให้ถือว่าไฟล์นั้นถูกต้องกว่าเสมอและแก้บรรทัดนี้ตาม
  - **Collection ที่สร้าง/ใช้งานจริงแล้ว:** `units`, `users` (เตรียม schema ไว้ล่วงหน้า ยังไม่ได้ใช้จริงจนกว่าจะต่อ Firebase Auth — BL-024), `drugItems`, `requisitions` (มี subcollection `lineItems` และ `approvalRecords`), `counters` (implementation-only ใช้ atomic increment สร้าง `requisitionCode`), `safetyStockThresholds`, `goodsReceiptRecords`
  - **Collection ที่ยังไม่ได้สร้าง** (สำรองไว้ตาม `01-requirements/06-data-model.md` §3.7/§3.9/§3.12–3.19 รอหน้าจอที่ต้องใช้จริง): `manualForecastAdjustments`, `historicalUsageRecords`, `inventoryBalances`, `notificationEvents`, `exportFiles`, `businessAuditLog`, `systemAccessLog`, `printableRequisitionDocuments`
- **สถานะ (`status`) ทั้งหมดของคำขอเบิก (Requisition)** ตาม `01-requirements/06-data-model.md` §5: `pending_level1` (รอการอนุมัติระดับ 1) → `pending_level2` (รอการอนุมัติระดับ 2) → `approved` (อนุมัติแล้ว) → `ready_to_export` (พร้อมส่งออก) → `dispensed` (จ่ายแล้ว) เป็นลำดับหลัก, `rejected` (ปฏิเสธ) แยกออกจากลำดับได้จากทุกขั้นก่อน `dispensed` — enum จริงใน Firestore หยุดที่ `dispensed` เท่านั้น **ไม่มีค่า "รับแล้ว" แยกเป็น enum** หน้ารายการคำนวณสถานะ "รับแล้ว" จากการมี doc ใน `goodsReceiptRecords` ที่อ้างอิงคำขอนั้นแทน (ดู `app/README.md` หัวข้อ "รับแล้ว")
- **Firebase Authentication จริงแล้ว (BL-024) และ Firestore Security Rules ที่กรองตาม `role`/`unitId` publish จริงแล้ว (20260909)** — `firestore.rules` deploy ผ่าน Firebase CLI แล้ว (ติดตั้งแล้วในเครื่อง ใช้คำสั่ง `firebase.cmd` บน Windows PowerShell เพราะ execution policy เริ่มต้นบล็อก shim `.ps1`) — ทดสอบ RBAC checklist ตามที่ระบุไว้ใน `app/README.md` หัวข้อ "ความปลอดภัย" ผ่านแล้วเช่นกัน (cross-unit isolation, ปฏิเสธ optimistic-lock ผิดเวอร์ชัน, บังคับ login) ไฟล์ config ที่ใช้ deploy กฎคือ `firebase.json`/`.firebaserc` ที่ root (ชี้ไปโปรเจกต์ `syncsmart-98d1e`) — มีบัญชี `role: "admin"` สร้างไว้แล้วผ่าน Console (Auth + `users/{uid}` doc) ก่อน publish ตามที่ README กำหนด
- **Hosting ของ `app/` — deploy จริงแล้ว 2 จุดพร้อมกัน (ทั้งคู่ชี้ Firestore/Auth โปรเจกต์ `syncsmart-98d1e` เดียวกัน):** (1) **Cloudflare** (ย้ายมาเป็นหลัก 20260914 ตั้งใจเชื่อม GitHub repo `6910127-warang/sync_smart-oneshot` branch `main` เพื่อ auto-deploy ทุกครั้งที่ push — **แต่ตอนตั้งค่าจริงเชื่อมผิดไปที่ repo พี่น้องชื่อ `6910127-warang/sync_smart` แทน** จึงไม่มี build อัตโนมัติเกิดขึ้นเลยตั้งแต่ 20260916 เป็นต้นมา ค้นพบ+แก้ไขแล้ว 20260920 — ดูรายละเอียดในหัวข้อ "เฟสปัจจุบัน" อัปเดต 20260920) — Live URL: `https://sync-smart.thiphbuymepharmacy.workers.dev` (config: `app/wrangler.toml`/`app/_worker.js`, Root directory บน Cloudflare ตั้งเป็น `/app`) (2) **Firebase Hosting** (ของเดิมก่อนย้าย ยังปล่อยขนานกันไว้ไม่ได้ปิด) — Live URL: `https://syncsmart-98d1e.web.app` (config: `firebase.json`/`.firebaserc` ที่ root, hosting public dir เป็น `app`) — ทั้งสองจุด exclude `seed.html` ออกจากการ host จริงเพราะเป็น dev tool เท่านั้น รายละเอียดขั้นตอน deploy เต็มดูใน `app/README.md` หัวข้อ "Hosting — ย้ายไป Cloudflare"

**วิธีสร้าง/อัปเดต Prototype:** ใช้ skill `/build-prototype` (`.claude/skills/build-prototype/`) ซึ่งจะส่งต่อให้ subagent `prototype-builder` (`.claude/agents/prototype-builder.md`) โดยตรง — สร้างหน้าจอ static HTML/CSS ภายใต้ `prototype/{role-slug}/{screen-slug}.html` (คลิกไปมาระหว่างหน้าได้ตาม happy path) โดยยึด DESIGN.md เป็น source of truth ของสี/ฟอนต์/spacing/component ทั้งหมด และดึงเนื้อหาหน้าจอจาก `02-feature-list.md`, `backlog.md`, `04-test-design/acceptance-criteria.md`, และ `03-user-journey/*.md` — ห้ามใช้ข้อมูลยา NCD จริงในหน้าจอตัวอย่าง (ใช้ placeholder เท่านั้น) รองรับการระบุเจาะจง (role, FT-ID, หรือ journey step เดียว) เนื่องจาก Prototype เต็มรูปแบบ (4 role × สูงสุด 25 Feature) มีขนาดใหญ่มาก ถ้าไม่ระบุขอบเขต skill จะถามยืนยันกลยุทธ์การสร้างก่อนเริ่มเสมอ ทะเบียนหน้าจอทั้งหมดดูที่ `prototype/screen-map.md`

**หมายเหตุการดู Prototype:** เปิดไฟล์ HTML ตรงๆ (`file://`) ในบาง preview tool อาจไม่โหลด CSS ให้ (relative path ของ `<link>` resolve ไม่ได้เมื่อ tool อ่านไฟล์เป็น static snapshot) — ให้รันผ่าน local static server แทน มี config พร้อมใช้แล้วที่ `.claude/launch.json` (ชื่อ config: `prototype`, serve โฟลเดอร์ `prototype/` ที่ port 4173)

**วิธีตรวจสอบว่า Prototype ยังสอดคล้องกับทั้ง chain หรือไม่ (โดยเฉพาะหลังมีคนแก้ไขหน้าจอ Prototype เอง นอกเหนือจาก `/build-prototype`):** ใช้ skill `/check-prototype-sync` (`.claude/skills/check-prototype-sync/`) ซึ่งจะส่งต่อให้ subagent `prototype-consistency-checker` (`.claude/agents/prototype-consistency-checker.md`) โดยตรง — ตรวจจับว่าหน้าจอไหนถูกแก้ไขตั้งแต่ตรวจครั้งก่อน (เทียบ content hash เก็บไว้ที่ `prototype/consistency-check-log.md`) แล้วเทียบเนื้อหาหน้าจอกับ spec/backlog/feature-list/journey/AC/test-case/test-plan ทั้งหมด — แก้ไขจุดที่ชัดเจนให้อัตโนมัติ (เอกสารตกยุคเทียบกับสิ่งที่ตัดสินใจไปแล้ว) แต่ **ห้ามสรุปเองว่าพฤติกรรมใหม่ที่เห็นใน Prototype คือ requirement จริง** — ต้องถามผู้ใช้ก่อนเสมอว่าจะ formalize เป็น requirement จริง (แนะนำให้ใช้ `/new-requirement`) หรือเป็นการแก้ไข Prototype เองที่ยังไม่ตั้งใจ (แนะนำให้รัน `/build-prototype` ทับกลับ) ทำงานคนละทิศทางจาก `/check-backlog-sync` (เช็คนี้ถือว่า Prototype อาจสะท้อนการตัดสินใจใหม่ที่ยังไม่ถูกบันทึกขึ้นไปด้านบน แทนที่จะถือว่า spec/backlog เป็นความจริงเสมอ) จึงไม่ถูก chain รวมกับ `/check-backlog-sync` เหมือนกับ `/build-prototype`

**วิธีทำทั้ง chain รวดเดียวตั้งแต่ raw requirement ถึง Test Design (ไม่ต้องเรียกทีละ skill):** ใช้ skill `/build-requirement-chain` (`.claude/skills/build-requirement-chain/`) — เป็น orchestrator ล้วนๆ ไม่มี logic เขียนเอกสารเอง แค่เรียก subagent ที่มีอยู่แล้ว 3 ตัวต่อกันตามลำดับบังคับ: (1) `requirement-writer` (raw requirement → spec + backlog) → (2) `feature-journey-builder` (backlog → feature-list + user-journey) → (3) `test-design-writer` (→ acceptance-criteria + test-plan + test-case) โดยแต่ละขั้นต้องรอให้เสร็จก่อนเริ่มขั้นถัดไป (เหมือนกับที่ `/check-backlog-sync` เรียงลำดับ subagent เดิม 2 ใน 3 ตัวนี้อยู่แล้ว) ใช้เมื่อมี raw requirement ใหม่และต้องการให้ผลไล่ลงไปถึง test design ในคำสั่งเดียว — ถ้าต้องการหยุดตรวจสอบระหว่างทางแต่ละขั้น ให้ใช้ `/new-requirement` ตามด้วย `/check-backlog-sync` (หรือ `/build-feature-journey`/`/build-test-design` แยก) แทน

**วิธีสร้าง/แก้ไข requirement ใหม่ (หยุดที่ spec+backlog เท่านั้น):** ใช้ skill `/new-requirement` (`.claude/skills/new-requirement/`) ซึ่งจะส่งต่อให้ subagent `requirement-writer` (`.claude/agents/requirement-writer.md`) เป็นผู้สัมภาษณ์ผู้ใช้ (ถามคำถามพร้อมตัวเลือกอย่างน้อย 3 แนวทางเสมอ), เขียน spec, อัปเดต backlog และ log ให้ครบตาม convention ด้านล่าง — ใช้ workflow นี้แทนการแก้ไฟล์เองตรงๆ ทุกครั้งที่มี raw requirement ใหม่จากผู้ใช้

**วิธีตรวจสอบว่าทั้ง chain sync กันหรือไม่ (Requirement → Backlog → Feature List → User Journey → Test Design):** ใช้ skill `/check-backlog-sync` (`.claude/skills/check-backlog-sync/`) — เรียก 3 subagent ต่อกันเป็นลำดับ: (1) `backlog-sync-checker` (`.claude/agents/backlog-sync-checker.md`) ตรวจสอบทุกไฟล์ spec เทียบกับ `01-requirements/backlog.md` ก่อน แก้ไขจุดที่ชัดเจนให้อัตโนมัติและถามผู้ใช้เมื่อพบจุดที่ต้องตัดสินใจ แล้ว (2) `feature-journey-builder` (`.claude/agents/feature-journey-builder.md`) ตรวจ `02-feature-list.md` และ `03-user-journey/*.md` เทียบกับ `backlog.md` เวอร์ชันล่าสุด (หลังขั้นที่ 1 แก้ไปแล้ว) แล้ว (3) `test-design-writer` (`.claude/agents/test-design-writer.md`) ตรวจ `04-test-design/` (acceptance-criteria.md, test-plan.md, test-cases/*.md) เทียบกับผลลัพธ์ของขั้นที่ 1-2 — ทำให้แก้ spec/backlog/feature-list/journey ที่จุดไหนก็ตาม แล้ว sync ไล่ลงไปจนถึง test-design ได้ในคำสั่งเดียว เรียกใช้เป็นระยะหลังแก้ spec/backlog หลายจุด หรือเมื่อสงสัยว่าจุดไหนใน chain นี้อาจไม่ตรงกันแล้ว

**วิธีสร้าง/อัปเดตเฉพาะ Feature List และ User Journey (ไม่ต้องตรวจ spec↔backlog ใหม่):** ใช้ skill `/build-feature-journey` (`.claude/skills/build-feature-journey/`) ซึ่งจะส่งต่อให้ subagent `feature-journey-builder` โดยตรง — สร้าง `01-requirements/02-feature-list.md` (จัดกลุ่ม backlog item เป็น Feature พร้อม MoSCoW ที่ roll-up จาก priority ของ backlog item) และ `01-requirements/03-user-journey/{role}-journey.md` (Mermaid diagram ต่อ role พร้อมคำอธิบาย mapping กลับไปยัง FR/US/BL-ID) ถ้ายังไม่มีไฟล์จะสร้างใหม่ ถ้ามีอยู่แล้วจะตรวจ+แก้ส่วนที่ไม่ตรงกับ backlog ปัจจุบัน — subagent ตัวนี้ถูกเรียกโดยอัตโนมัติเป็นขั้นที่ 2 ของ `/check-backlog-sync` อยู่แล้วด้วย ใช้ `/build-feature-journey` ตรงๆ เฉพาะเมื่อต้องการรีเฟรชแค่ feature-list/journey เร็วๆ โดยไม่ต้องตรวจ spec↔backlog ใหม่ทั้งหมด

**วิธีสร้าง/อัปเดต Acceptance Criteria, Test Plan, และ Test Case:** ใช้ skill `/build-test-design` (`.claude/skills/build-test-design/`) ซึ่งจะส่งต่อให้ subagent `test-design-writer` (`.claude/agents/test-design-writer.md`) โดยตรง — สร้าง `01-requirements/04-test-design/acceptance-criteria.md` (ขยาย AC แบบย่อใน backlog.md เป็น Given-When-Then หลาย scenario ต่อ BL-ID), `01-requirements/04-test-design/test-plan.md` (ภาพรวมกลยุทธ์ทดสอบ 1 ไฟล์ต่อโปรเจกต์: scope, ประเภทการทดสอบ, environment, risk management, entry/exit criteria) และ `01-requirements/04-test-design/test-cases/{feature-slug}.md` (test case แบบ step-by-step ต่อ Feature พร้อม test id, pre-condition, test steps, expected result, test data, และอ้างอิงกลับไปยัง requirement/AC) — รองรับการระบุเจาะจง (เฉพาะ BL-ID, FT-ID, Epic, หรือ artifact เดียว) ถ้าไม่ระบุจะครอบคลุมทั้งหมด ถ้ายังไม่มีไฟล์จะสร้างใหม่ ถ้ามีอยู่แล้วจะตรวจ+แก้ส่วนที่ไม่ตรงกับ backlog/feature-list/journey ปัจจุบัน — subagent ตัวนี้ถูกเรียกโดยอัตโนมัติเป็นขั้นที่ 3 ของ `/check-backlog-sync` อยู่แล้วด้วย ใช้ `/build-test-design` ตรงๆ เฉพาะเมื่อต้องการรีเฟรช/สร้างเฉพาะ test-design เร็วๆ โดยไม่ต้องตรวจทั้ง chain ใหม่

**วิธีสร้าง/อัปเดต High-Level Architecture (เชิงแนวคิด ไม่ผูกมัดกับ technical stack):** ใช้ skill `/build-architecture` (`.claude/skills/build-architecture/`) ซึ่งจะส่งต่อให้ subagent `architecture-writer` (`.claude/agents/architecture-writer.md`) โดยตรง — สร้าง/ปรับปรุง `01-requirements/05-architecture.md` (system context diagram, องค์ประกอบเชิงตรรกะ, data flow ต่อ role ที่ derive จาก `03-user-journey/*.md`, จุดเชื่อมต่อระบบภายนอก, ข้อมูลเชิงแนวคิดหลัก, ประเด็น NFR) — **ห้ามเอ่ยชื่อ technology/framework/database/cloud vendor ใดๆ ในเอกสารนี้เด็ดขาด** เพราะเฟสปัจจุบันยังไม่เข้าสู่การพัฒนา (ดู "เฟสปัจจุบัน" ด้านบน) ถ้าจุดไหนกำกวม (ระดับความละเอียดของ component, จะรวมมุมมอง deployment เชิงแนวคิดหรือไม่ ฯลฯ) agent จะถามผู้ใช้พร้อมตัวเลือกอย่างน้อย 3 แนวทาง **พร้อมข้อดี/ข้อเสียของแต่ละแนวทาง** เสมอ ถ้ายังไม่มีไฟล์จะสร้างใหม่ ถ้ามีอยู่แล้วจะตรวจ+แก้ส่วนที่ไม่ตรงกับ backlog/feature-list/journey ปัจจุบัน — ไม่ได้ถูก chain รวมกับ `/check-backlog-sync` โดยอัตโนมัติ (เรียกแยกตามต้องการ)

**วิธีสร้าง/อัปเดต Data Model (ER Diagram + รายละเอียดตาราง) และ API Spec (เชิงแนวคิด ไม่ผูกมัดกับ technical stack):** ใช้ skill `/build-data-api-spec` (`.claude/skills/build-data-api-spec/`) ซึ่งจะส่งต่อให้ subagent `data-api-designer` (`.claude/agents/data-api-designer.md`) โดยตรง — สร้าง/ปรับปรุงเอกสารคู่กัน 2 ไฟล์: `01-requirements/06-data-model.md` (ER Diagram, รายละเอียดฟิลด์ต่อตาราง/entity, ความสัมพันธ์, ข้อมูลอ้างอิง/รายการคงที่) และ `01-requirements/07-api-spec.md` (operation ที่จัดกลุ่มตาม resource หรือ user journey, สิทธิ์การเข้าถึงตาม role, cross-reference กลับไปยัง entity ใน data model) โดย derive จาก `backlog.md`, `02-feature-list.md`, `03-user-journey/*.md`, `05-architecture.md` (ถ้ามี), และ spec ที่เกี่ยวข้อง — **ห้ามเอ่ยชื่อ database engine, ORM, API protocol/style (REST/GraphQL/gRPC ฯลฯ), หรือกลไก authentication ใดๆ ในเอกสารทั้งสองนี้เด็ดขาด** (ชนิดข้อมูลใช้คำเชิงแนวคิดเช่น "ข้อความ"/"ตัวเลข"/"วันที่" แทน SQL type) เพราะเฟสปัจจุบันยังไม่เข้าสู่การพัฒนา (ดู "เฟสปัจจุบัน" ด้านบน) ถ้าจุดไหนกำกวม (ระดับความละเอียดของตาราง, convention การตั้งชื่อฟิลด์, หลักการจัดกลุ่ม operation ฯลฯ) agent จะถามผู้ใช้พร้อมตัวเลือกอย่างน้อย 3 แนวทาง **พร้อมข้อดี/ข้อเสียของแต่ละแนวทาง** เสมอ ถ้ายังไม่มีไฟล์จะสร้างใหม่ ถ้ามีอยู่แล้วจะตรวจ+แก้ส่วนที่ไม่ตรงกับ backlog/feature-list/journey/architecture ปัจจุบัน (รวมถึงตรวจความสอดคล้องระหว่างสองไฟล์นี้เอง) — ไม่ได้ถูก chain รวมกับ `/check-backlog-sync` โดยอัตโนมัติ (เรียกแยกตามต้องการ)

**วิธีสร้าง/อัปเดต Detailed Design (เชิงแนวคิด มี sequence diagram เป็นอย่างน้อย ไม่ผูกมัดกับ technical stack):** ใช้ skill `/build-detailed-design` (`.claude/skills/build-detailed-design/`) ซึ่งจะส่งต่อให้ subagent `detailed-design-writer` (`.claude/agents/detailed-design-writer.md`) โดยตรง — สร้าง/ปรับปรุงไฟล์หนึ่งไฟล์ต่อ Feature ที่ `01-requirements/08-detailed-design/{feature-slug}.md` (sequence diagram ต่อ scenario อย่างน้อย 1 ไฟล์ต่อ Feature, component-interaction, error/exception flow, และ lifecycle/state diagram เมื่อเกี่ยวข้อง) พร้อมทะเบียนรวมที่ `01-requirements/08-detailed-design/README.md` โดย derive จาก `backlog.md`, `02-feature-list.md`, `03-user-journey/*.md`, `04-test-design/acceptance-criteria.md`, `05-architecture.md`/`06-data-model.md`/`07-api-spec.md` (ถ้ามี) — **ห้ามเอ่ยชื่อ technology/protocol/เทคนิคการ implement ใดๆ ในเอกสารนี้เด็ดขาด** (รวมถึงในแผนภาพ sequence diagram เอง — participant ต้องเป็น role/องค์ประกอบเชิงตรรกะ/ระบบภายนอกเท่านั้น ไม่ใช่ class/service/table name) เพราะเฟสปัจจุบันยังไม่เข้าสู่การพัฒนา (ดู "เฟสปัจจุบัน" ด้านบน) ถ้าจุดไหนกำกวม (ความละเอียดของ scenario ต่อ Feature, วิธีแทน participant เมื่อยังไม่มี architecture doc, จะรวม state diagram หรือไม่ ฯลฯ) agent จะถามผู้ใช้พร้อมตัวเลือกอย่างน้อย 3 แนวทาง **พร้อมข้อดี/ข้อเสียของแต่ละแนวทาง** เสมอ รองรับการระบุเจาะจง (FT-ID, BL-ID, หรือ Epic) เนื่องจากการสร้างครบทุก Feature (สูงสุด 25 Feature) มีขนาดใหญ่ ถ้าไม่ระบุขอบเขต skill จะถามยืนยันกลยุทธ์การสร้างก่อนเริ่มเสมอ ถ้ายังไม่มีไฟล์จะสร้างใหม่ ถ้ามีอยู่แล้วจะตรวจ+แก้ส่วนที่ไม่ตรงกับ backlog/feature-list/journey/AC/architecture/data-model/API-spec ปัจจุบัน — ไม่ได้ถูก chain รวมกับ `/check-backlog-sync` โดยอัตโนมัติ (เรียกแยกตามต้องการ)

**หมายเหตุสำคัญ — ป้องกันเอกสาร 05-08 หลุด sync แบบเงียบๆ:** `/build-architecture`, `/build-data-api-spec`, และ `/build-detailed-design` ทั้ง 3 ตัวไม่ได้ถูกเรียกอัตโนมัติเป็นส่วนหนึ่งของ `/check-backlog-sync` (ต่างจาก `/build-feature-journey` และ `/build-test-design` ที่ถูก chain ไว้แล้ว) — หลังแก้ไข backlog/feature-list/user-journey ครั้งใหญ่ ต้องพิจารณารันทั้ง 3 skill นี้แยกต่างหากด้วยเสมอ ไม่เช่นนั้นเอกสาร High-Level Architecture, Data Model, API Spec, และ Detailed Design จะ stale โดยไม่มีกลไกใดเตือนอัตโนมัติ

## โครงสร้างเอกสาร (Document Structure)

- **`DESIGN.md`** — Design System หลักของโปรเจกต์ (สี/ฟอนต์/spacing/component/UX rules) — source of truth ของ Prototype ทั้งหมด แก้ไขได้ตรงๆ ตามที่ผู้ใช้ต้องการ (ไม่มี skill ดูแลอัตโนมัติ)
- **`01-requirements/01-spec/{YYYYMMDD}-{RUNNING_NO}-{topic-slug}.md`** — เอกสาร requirement แยกตามหัวข้อ/Epic หนึ่งไฟล์ต่อหนึ่งเรื่อง (ไม่ใช่ไฟล์รวมเดียวเหมือนเดิม) โดย `RUNNING_NO` นับต่อเนื่องทั้งโปรเจกต์ (ไม่รีเซ็ต) และ `topic-slug` เป็น kebab-case ภาษาอังกฤษ
- **`01-requirements/backlog.md`** — Product Backlog หลัก อ้างอิงกลับไปยัง spec แต่ละไฟล์
- **`01-requirements/02-feature-list.md`** — Feature List (มุมมองระดับ feature ที่จัดกลุ่มมาจาก backlog item พร้อม MoSCoW roll-up) — ดูแลโดย `/build-feature-journey`, ไม่ต้องแก้มือ
- **`01-requirements/03-user-journey/{role}-journey.md`** — User Journey แยกไฟล์ต่อ role (Mermaid diagram + คำอธิบาย mapping กลับ FR/US/BL-ID) — ดูแลโดย `/build-feature-journey`, ไม่ต้องแก้มือ
- **`01-requirements/04-test-design/acceptance-criteria.md`** — Acceptance Criteria แบบ Given-When-Then เต็มรูปแบบต่อ backlog item (ขยายจาก AC แบบย่อใน backlog.md) — ดูแลโดย `/build-test-design`, ไม่ต้องแก้มือ
- **`01-requirements/04-test-design/test-plan.md`** — Test Plan ภาพรวมกลยุทธ์ทดสอบ 1 ไฟล์ต่อโปรเจกต์ (scope, ประเภทการทดสอบ, environment, risk management, entry/exit criteria) — ดูแลโดย `/build-test-design`, ไม่ต้องแก้มือ
- **`01-requirements/04-test-design/test-cases/{feature-slug}.md`** — Test Case แบบ step-by-step ต่อ Feature (test id, pre-condition, test steps, expected result, test data, อ้างอิง requirement/AC) — ดูแลโดย `/build-test-design`, ไม่ต้องแก้มือ
- **`01-requirements/05-architecture.md`** — High-Level Architecture เชิงแนวคิด (system context, องค์ประกอบเชิงตรรกะ, data flow ต่อ role, จุดเชื่อมต่อระบบภายนอก, NFR) — **ไม่ผูกมัดกับ technology stack ใดๆ** — ดูแลโดย `/build-architecture`, ไม่ต้องแก้มือ
- **`01-requirements/06-data-model.md`** — Data Model / Database Schema เชิงแนวคิด (ER Diagram, รายละเอียดฟิลด์ต่อตาราง/entity, ความสัมพันธ์, ข้อมูลอ้างอิง) — **ไม่ผูกมัดกับ database engine หรือ technology stack ใดๆ** — ดูแลโดย `/build-data-api-spec`, ไม่ต้องแก้มือ
- **`01-requirements/07-api-spec.md`** — API Spec เชิงแนวคิด (operation จัดกลุ่มตาม resource/journey, สิทธิ์การเข้าถึงตาม role, cross-reference กลับไปยัง `06-data-model.md`) — **ไม่ผูกมัดกับ API protocol/style หรือกลไก authentication ใดๆ** — ดูแลโดย `/build-data-api-spec`, ไม่ต้องแก้มือ
- **`01-requirements/08-detailed-design/{feature-slug}.md`** — Detailed Design เชิงแนวคิดต่อ Feature (sequence diagram ต่อ scenario เป็นอย่างน้อย, component-interaction, error/exception flow, lifecycle/state diagram เมื่อเกี่ยวข้อง) พร้อม `01-requirements/08-detailed-design/README.md` เป็นทะเบียนไฟล์ทั้งหมด — **ไม่ผูกมัดกับ technology stack หรือ protocol ใดๆ** — ดูแลโดย `/build-detailed-design`, ไม่ต้องแก้มือ
- **`prototype/{role-slug}/{screen-slug}.html`** — หน้าจอ Prototype แบบ static HTML/CSS ต่อ role (คลิกไปมาได้ตาม happy path, สไตล์ตาม DESIGN.md) พร้อม `prototype/screen-map.md` เป็นทะเบียนหน้าจอทั้งหมด — ดูแลโดย `/build-prototype`, ไม่ต้องแก้มือ
- **`log/{YYYYMMDD}-log.md`** — บันทึกสรุปงานที่ทำในแต่ละวัน (สร้าง/แก้ spec ไหน, ถามอะไรไปบ้าง, backlog เปลี่ยนอะไร)

**Spec ปัจจุบันทั้งหมด** (แตกจาก Epic ตามขอบเขต — เรียงตามความสำคัญ):

| #   | หัวข้อ                                                          | ไฟล์                                                                                                                                 |
| --- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 001 | ภาพรวมโครงการ/ที่มาปัญหา/glossary/stakeholder                   | [20260816-001-project-scope-and-problem-background.md](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md) |
| 002 | เบิก-จ่ายยา NCD (Core)                                          | [20260816-002-ncd-drug-requisition-core.md](01-requirements/01-spec/20260816-002-ncd-drug-requisition-core.md)                       |
| 003 | พยากรณ์สต็อก / แจ้งเตือนยาใกล้หมด                               | [20260816-003-stock-forecast-alerts.md](01-requirements/01-spec/20260816-003-stock-forecast-alerts.md)                               |
| 004 | รายงาน/Dashboard ผู้บริหาร                                      | [20260816-004-management-dashboard.md](01-requirements/01-spec/20260816-004-management-dashboard.md)                                 |
| 005 | เชื่อมต่อระบบเดิม (JHCIS/myPCU ที่ รพ.สต., INVC ที่ รพ.แม่ข่าย) | [20260816-005-legacy-system-integration.md](01-requirements/01-spec/20260816-005-legacy-system-integration.md)                       |
| 006 | ระบบและความปลอดภัย (RBAC, NFR)                                  | [20260816-006-rbac-and-security-nfr.md](01-requirements/01-spec/20260816-006-rbac-and-security-nfr.md)                               |

**สิ่งที่ยังไม่ยืนยัน (Open Items) ต้องดูในแต่ละไฟล์ spec โดยตรง** (หัวข้อ "สิ่งที่ยังไม่ยืนยัน" ท้ายไฟล์) — **อย่าเก็บรายการ open questions ซ้ำไว้ใน CLAUDE.md** เพราะจะหลุด sync กับความจริงเมื่อมีการตอบคำถามไปแล้ว (เคยเกิดขึ้นมาแล้วสี่ครั้งกับไฟล์นี้เอง — ครั้งล่าสุด 20260825 ช่วงเช้าที่บรรทัดนี้เองยังอ้างว่า BL-009b ยังไม่ยืนยัน ทั้งที่ปิดไปแล้ววันเดียวกันนั้นเอง จากนั้นบรรทัดนี้ถูกแก้ให้ชี้ไปที่ BL-038/BL-043 แทน แล้วสองข้อนั้นก็ถูกปิดในบทสนทนาเดียวกันอีกไม่กี่นาทีถัดมา) ไม่มีจุดใดที่ต้องไฮไลต์เป็นพิเศษ ณ ตอนนี้ — ทุก Epic ปิด open item หลักของตัวเองครบแล้ว (ดูรายละเอียด/ประวัติเต็มใน log/20260825-log.md)

## แนวทางการเขียนเอกสาร (Document Conventions)

- ใช้ภาษาไทยเป็นหลัก คำศัพท์เทคนิค/ตัวย่อภาษาอังกฤษที่วงการใช้ทั่วไป (SRS, User Story, Backlog, Sprint) คงไว้ตามเดิมได้
- **User Story format:** `ในฐานะ [role] ฉันต้องการ [ความต้องการ] เพื่อ [เหตุผล/คุณค่า]` พร้อม Acceptance Criteria แบบ Given/When/Then หรือ checklist
- **Backlog item ควรมี:** ID (`BL-XXX` ต่อเนื่อง ไม่มีวันรีเซ็ต), Epic/หมวดหมู่, User Story, Acceptance Criteria, Priority (MoSCoW: Must/Should/Could/Won't), หมายเหตุด้าน dependency
- **คำศัพท์เฉพาะ (Glossary):** เก็บรวมไว้ที่เดียวใน spec 001 ([20260816-001-project-scope-and-problem-background.md](01-requirements/01-spec/20260816-001-project-scope-and-problem-background.md)) ไม่ต้องซ้ำในไฟล์อื่น — เพิ่มคำใหม่ที่อาจกำกวมทันทีที่พบ
- **แก้ไข vs. สร้างไฟล์ spec ใหม่:** เป็นการวิเคราะห์ต่อครั้ง ไม่ใช่กฎตายตัว — ดูตรรกะเต็มใน `.claude/agents/requirement-writer.md` (แก้ไฟล์เดิมถ้าเป็นการต่อยอด/แก้ไขเรื่องเดิม, สร้างไฟล์ใหม่ถ้าเป็นหัวข้อ/Epic ใหม่จริงๆ)
- Versioning ของแต่ละไฟล์ spec เก็บผ่าน git log และหัวข้อ "บันทึกการอัปเดต" ในตัวไฟล์เอง ไม่ใช่ชื่อไฟล์ (ห้ามตั้งชื่อ `..._v2_final.md`)

## กฎการทำงานร่วมกับ Claude ในโปรเจกต์นี้

- **ห้ามสมมติข้อมูลทางคลินิก/นโยบายยา NCD เอง** — เรื่องเกณฑ์ยา, ปริมาณสำรอง (safety stock), เกณฑ์แจ้งเตือน ต้องถามผู้ใช้ (เภสัชกรเจ้าของระบบ) เสมอ เพราะเป็นความรู้เฉพาะทางที่ผู้ใช้มีอำนาจตัดสินใจ
- **ห้ามสมมติข้อมูลระบบภายนอกเอง** (ชื่อ/เวอร์ชัน/รูปแบบการเชื่อมต่อของ HOSxP, INVC, JHCIS, myPCU หรือระบบอื่นที่ยังไม่เคยถูกยืนยัน) — เคยเกิดขึ้นมาแล้วครั้งหนึ่งในโปรเจกต์นี้ที่สมมติผิดว่า รพ.แม่ข่ายใช้ HOSxP ทั้งที่จริงใช้ INVC ต้องถามให้ชัดก่อนเขียนลงเอกสารเป็นข้อสรุปเสมอ
- เมื่อเขียน requirement หรือ backlog แล้วเจอจุดที่ขอบเขตไม่ชัด ให้ **หยุดถามก่อน** แทนที่จะเดาแล้วเขียนลงเอกสารเป็นข้อสรุป — และเมื่อถาม ให้เสนอตัวเลือกอย่างน้อย 3 แนวทางเสมอ (ไม่ใช่คำถามปลายเปิด)
- ไม่ต้องสร้างโค้ด, ไม่ต้อง setup โปรเจกต์ซอฟต์แวร์ (package.json, framework ฯลฯ) ในเฟสนี้ เว้นแต่ผู้ใช้ขอเจาะจง
- เอกสาร spec แต่ละไฟล์ที่แก้ไข ให้แก้ไฟล์เดิมแทนสร้างไฟล์ใหม่ซ้ำซ้อนเมื่อเป็นเรื่องเดียวกัน (ดูตรรกะ new-vs-amend ด้านบน)
- **ห้ามใส่ credential/secret จริงลงไฟล์ที่จะ push ขึ้น git** เช่น service account key, admin SDK key, API token/channel secret ของระบบภายนอก (LINE OA, INVC, JHCIS ฯลฯ) — ต้องเก็บไว้นอก repo เท่านั้น (environment variable หรือไฟล์ local ที่อยู่ใน `.gitignore`) **ข้อยกเว้น:** `app/firebase-config.js` ที่มี Firebase `apiKey` อยู่แล้วไม่ถือว่าผิดกฎนี้ เพราะเป็น client-side identifier ตามมาตรฐานของ Firebase เอง ไม่ใช่ secret (ความปลอดภัยจริงมาจาก Firestore Security Rules ไม่ใช่การซ่อนค่านี้ — แต่ตอนนี้ยังไม่ได้ตั้ง rule ดู "สถาปัตยกรรมและคำสั่งที่ใช้ใน `app/`" ด้านบน)
